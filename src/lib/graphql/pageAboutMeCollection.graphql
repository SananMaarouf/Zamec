query pageAboutMe($locale: String, $preview: Boolean) {
  pageAboutMeCollection(locale: $locale, preview: $preview) {
    items {
      sys {
        id
      }
      title
      description {
        json
      }
      portrait {
        url
        width
        height
      }
    }
  }
}
